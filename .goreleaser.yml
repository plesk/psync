project_name: psync
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - darwin
checksum:
  name_template: 'checksums.txt'
snapshot:
  name_template: "{{ .Tag }}"
changelog:
  filters:
    exclude:
      - '^TECH'
